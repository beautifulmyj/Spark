package com.apple.chapter03

object Scala05_Ops {

    def main(args: Array[String]): Unit = {


        // TODO Scala中其实没有运算符，有的是方法
        // scala是一个完全面向对象的语言，万物皆对象，数字也是对象
        //val i = 1.+(1)
        // 如果方法用于运算，那么点可以省略
        //val i = 1 +(1)
        // 如果方法只有一个参数，或没有参数，那么小括号可以省略
        //val j = 1 + 1

        //1 toString

        //val i = 1 + 1
       // println("i = " + i)

        //1.to(5)
        //1 to 5

        "a".->(1)

    }
}
